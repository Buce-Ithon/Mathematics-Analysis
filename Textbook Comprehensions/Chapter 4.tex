%\documentclass{Math_Note}
%\setlength{\parindent}{2em}

%\title{\textbf{Mathmatical Analysis Zorich Comprehensions Chapter 4}}
%\author{Buce-Ithon}
%\newdateformat{mydate}{\twodigit{\THEDAY}{ }\shortmonthname[\THEMONTH], \THEYEAR}
%\date{\today}

%\begin{document}
% Title page
%\maketitle

% Content
%\newpage
%\tableofcontents
%\newpage

% \setcounter{section}{-1}
\newpage
\section{Chapter 4.Continuous Functions}
\subsection{Comprehensions}
This chapter discusses the continuity of functions (including discontinuities when not continuous). First, we define the concepts of continuity at a point, continuity on an interval, and one-sided continuity, 
and introduces the concept of points of discontinuity ($2$ types: depending on whether the one-sided limits exist). Further, it defines the stronger concept of uniform continuity. Then, it presents the arithmetic 
operations and composition of continuous functions, and provides $3$ global properties of continuous functions (on closed intervals: 1. Bolzano-Cauchy intermediate value theorem, 2. Weierstrass extreme value theorem 
(boundedness), 3. Cantor-Heine uniform continuity theorem). Additionally, Zorich also covers the continuity of monotonic functions in the book (which is special as before) (types of discontinuities for monotonic 
functions (only the first kind) and their number (at most countable), and the relationship between continuity and the range (intermediate value property) for monotonic functions derived from this). Finally, it 
introduces the inverse function theorem, which is very intuitively understood (on closed intervals: existence $\Leftrightarrow$ injective $\Leftrightarrow$ strictly monotonic, preserving continuity).
\newline\newline
\fbox{\begin{minipage}{0.95\textwidth}
\textbf{Continuity of 2 Special fuctions: }
\begin{enumerate}
    \item[1.] Dirichlet function $\mathcal{D}(x) = \begin{cases} 1, & x \in \mathbb{Q} \\ 0, & x \in \mathbb{R} \setminus \mathbb{Q} \end{cases}$ \\
    Equivalent expression: $\forall x \in \mathbb{R}, \mathcal{D}(x) = \lim_{k \to \infty} (\lim_{j \to \infty} (\cos(k!x))^{2j})$ \\
    Properties: Every point is a discontinuity point of the second kind (no limit anywhere, discontinuous, non-differentiable); has every positive rational number as its period (thus no minimal positive period); 
    not Riemann integrable on any bounded interval, but Lebesgue integrable on $[0,1]$.
    \item[2.] Riemann function $\mathcal{R}(x) = \begin{cases} \frac{1}{n}, & x = \frac{m}{n} \in \mathbb{Q} \setminus \{0\} \\ 0, & x \in \mathbb{R} \setminus \mathbb{Q} \cup \{0\} \end{cases}$ \\
    The number-theoretic essence of the first kind discontinuity points of the Riemann function: $\forall a \in \mathbb{R}, N \in \mathbb{N}$, in $U(a)$ there are only finitely many rational numbers $m/n$ 
    (assumed to be in irreducible form) with $n < N$, i.e., $\frac{1}{n} > \frac{1}{N}$. \\
    Properties: $\mathcal{R}(x)$ is continuous at all irrational points and has first kind discontinuities at all rational points; the Riemann function is Riemann integrable.
\end{enumerate}
\end{minipage}}

\subsection{Problems Sets}
\begin{prb}
    (C4S2Q1)
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{qsol*}
\begin{enumerate}
    \item [a)] $f \in C(A), B \subset A \implies f|_B \in C(B)$.
    \item [b)] $f : E_1 \cup E_2 \to \mathbb{R}$, satisfying $f|_{E_i} \in C(E_i), i = 1, 2 \nRightarrow f \in C(E_1 \cup E_2)$, counterexample: $f(x) = \tan x$.
    \item [c)] $\forall N > 0, \{ \frac{m}{n} : n < N, (m, n) = 1, m, n \in \mathbb{Z}^+ \}$ is a finite set, i.e. $\exists U(x; \delta) (\forall x \in (0, 1))$ such that $\forall x_1, x_2 
    \in U(x; \delta), |R(x_1) - R(x_2)| \leq \frac{1}{N} < \varepsilon$ (Cauchy convergence criterion), then $\forall x_0 \in [0, 1], \lim_{x \to x_0} R(x) = 0$. (The proof method is consistent with 
    the discussion of discontinuity points of the Riemann function in Example 2 of C4 Summary.)
\end{enumerate}
\end{qsol*}
\end{minipage}}

\begin{prb}
    (C4S2Q2) \\
    If $f\in C[a,b]$, then $m(x)=\min_{a\leq t\leq x}f(t),\max_{a\leq t\leq x}f(t) \in C[a,b]$.
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{qsol*}
Note that $m(x) = \min_{a \leq t \leq x} f(t)$, $M(x) = \max_{a \leq t \leq x} f(t) \in C[a, b]$, and since each is monotonic, their ranges can be given. Then, by relating to the relationship 
between the continuity of monotonic functions and their ranges (intermediate value property), the proof is completed.
\end{qsol*}
\end{minipage}}

\begin{prb}
    (C4S2Q3)
    \begin{enumerate}
        \item[a)] On an open interval, the inverse function of a monotonic function exists and is continuous.
        \item[b)] Construct a monotonic function with a countable set of discontinuities.
        \item[c)] If $f: X \to Y$ and $f^{-1}: Y \to X$ are inverse functions of each other, and $f$ is continuous at $x_0 \in X$, then it does not imply that $f^{-1}$ is continuous at $y_0 = f(x_0)$.
    \end{enumerate}
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{sol*}
\begin{enumerate}
    \item[a)] Extend to the closed interval (taking one-sided limits at the endpoints) using the inverse function theorem, then restrict back to the open interval.
    \item[b)] $f(x) = [x]$.
    \item[c)] Actually, such a counterexample comes from the lack of closed interval restriction in the inverse function theorem. If $x_0$ is an isolated point, it is easy to construct an example.
\end{enumerate}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.24]{"./Figures/250805_1.png"}
\end{figure}
\end{sol*}
\end{minipage}}

\begin{prb}
    (C4S2Q4) \\
    \begin{enumerate}
        \item[a)] if $f \in C[a, b]$ and $g \in C[a, b]$, and, in addition, $f(a) < g(a)$ and $f(b) > g(b)$, then there exists a point $c \in [a, b]$ at which $f(c) = g(c)$;
        \item[b)] any continuous mapping $f : [0, 1] \rightarrow [0, 1]$ of a closed interval into itself has a fixed point, that is, a point $x \in [0, 1]$ such that $f(x) = x$;
        \item[c)] if two continuous mappings $f$ and $g$ of an interval into itself commute, that is, $f \circ g = g \circ f$, then they have a common fixed point;
        \item[d)] a continuous mapping $f : \mathbb{R} \rightarrow \mathbb{R}$ may fail to have a fixed point;
        \item[e)] a continuous mapping $f : ]0, 1[ \rightarrow ]0, 1[$ may fail to have a fixed point;
        \item[f)] if a mapping $f : [0,1] \rightarrow [0,1]$ is continuous, $f(0) = 0$, $f(1) = 1$, and $(f \circ f)(x) \equiv x$ on $[0,1]$, then $f(x)\equiv x$;
    \end{enumerate}
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{qsol*}
\begin{enumerate}
    \item[a)] Constrcut $h(x) = f(x) - g(x)$, then Bolzano-Cauchy intermediate value theorem applies.
    \item[b)] Constrcut $h(x) = f(x) - x$, then disccuss its values at $x=0$ and $x=1$, and using Bolzano-Cauchy IVT.
    \item[c)] /Insane/ The question should be modified to 'If 2 continuous maps $f$ and $g$ of a line segment into itself commute, i.e. $f\circ g=g\circ f$, then they donn't always have a common
    fixed point, though for linear maps and polynomials in general we have such a point.' \\
    /The counterexample was given by W. M. Boyce 1969, the proof for linear maps is easy using [contradiction], the proof for polynomials is a bit of hard \textcolor{silkred}{(wait to add)}./
    \item[d)] $f(x)=x+1$, $f(x)=e^x$ 
    \item[e)] $f(x)=\sin \frac{\pi}{2}x$, $f(x)=x^2$ 
    \item[f)] $1^{\circ}.$ $f$ is injection [contradiction] and surjection $\implies$ bijection, then we could get monotonic; $2^{\circ}.$ $f\equiv x\Leftrightarrow f\leq x \wedge f\geq x$ [contradiction].
\end{enumerate}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.24\textwidth]{"./Figures/250829_1.png"}
\end{figure}
\end{qsol*}
\end{minipage}}

\begin{prb}
    (C4S2Q5) \\
    Show that the set of values of any function that is continuous on a closed interval is a closed interval.
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{qsol*}
Weierstrass exreme value theorem + Bolzano-Cauchy intermediate value theorem.
\end{qsol*}
\end{minipage}}

\begin{prb}
    (C4S2Q6) \\
    Prove the following statements:
    \begin{enumerate}
        \item[a)] If a mapping $f : [0, 1] \rightarrow [0, 1]$ is continuous, $f(0) = 0$, $f(1) = 1$, and $f^n(x) := \underbrace{f \circ \ldots \circ f}_{n\ \text{factors}} (x) \equiv x$ on $[0, 1]$, 
        then $f(x) \equiv x$.
        \item[b)] If a function $f : [0, 1] \rightarrow [0, 1]$ is continuous and nondecreasing, then $\forall x \in [0, 1]$ at least one of the following situations must occur: either $x$ is 
        a fixed point, or $f^n(x)$ tends to a fixed point. (Here $f^n(x) = f \circ \ldots \circ f(x)$ is the nth iteration of $f$.)
    \end{enumerate}
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{sol*}
\begin{enumerate}
    \item[a)] Similiar (almost same) to C4S2Q4-f).
    \item[b)] $\forall x\in [0,1]$, $x_0=x, x_1=f(x), \cdots, x_n=f^n(x), \cdots$, \\
    then there are only 2 condition between $x$ and $f(x)$: \\
    1. $x=f(x)$ i.e. fixed point; \\
    2. $x\leq f(x)$ or $x\leq f(x)$, from monotonicity of $f(x)$ we know $\{x_n\}_{n=0}^{\infty}$ monotonic and bounded, then due to continuity of $f(x)$ we could find the limit (fixed point).
\end{enumerate}
\end{sol*}
\end{minipage}}

\begin{prb}
    (C4S2Q7) \\
    Let $f : [0, 1] \to \mathbb{R}$ be a continuous function such that $f(0) = f(1)$. Show that
    \begin{enumerate}
        \item[a)] $\forall n \in \mathbb{N}$ there $\exists $ a horizontal closed interval of length $\frac{1}{n}$ with endpoints on the graph of this function;
        \item[b)] if the number $l$ is not of the form $\frac{1}{n}$ there exists a function of this form on whose graph one cannot inscribe a horizontal chord of length $l$.
    \end{enumerate}
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{sol*}
\begin{enumerate}
    \item[a)] It $\Leftrightarrow$ $\forall n \in \mathbb{N}$, $\exists x \in [0,1-\frac{1}{n}]$, s.t. $f(x+\frac{1}{n})-f(x)=0$. \\
    Construct $g(x)=f(x+\frac{1}{n})-f(x)$, $x \in [0,1-\frac{1}{n}]$. \\
    Consider a partition of $[0,1-\frac{1}{n}]$ with $n$ points: $0, \frac{1}{n}, \dots , 1-\frac{1}{n}$ and their $g(x)$ values: \\
    $\begin{cases} g(0)=f(\frac{1}{n})-f(0)\\ g(\frac{1}{n})=f(\frac{2}{n})-f(\frac{1}{n})\\ \cdots\\ g(1-\frac{1}{n})=f(1)-f(1-\frac{1}{n}) \end{cases}$, $\Sigma$: $\sum_{k=0}^{n-1}g(\frac{k}{n})=f(1)-f(0)\equiv 0$ \\
    then $g(0), g(\frac{1}{n}),\dots , g(1-\frac{1}{n})$ either all $0$, or have at least $2$ different signs (then using Bolzano-Cauchy IVT), thus there always $\exists x \in [0,1-\frac{1}{n}]$, s.t. $g(x)=f(x+\frac{1}{n})-f(x)=0$. 
    \item[b)] $\forall l>0$, consider: $f(x)=\sin^2(\frac{\pi x}{r})-x \sin^2(\frac{\pi}{r}),\ \ x \in [0,1]$ \\
    Clearly, $f(0)=f(1)$. If $f(x)=f(x+r)$, then $r\sin^2(\frac{\pi}{r})=0$, which means $r=\frac{1}{n},\ n \in \mathbb{N}$. (by Paul Levy) \\
    /Development: Universal Chord Theorem (by Paul Levy)/
\end{enumerate}
\end{sol*}
\end{minipage}}

\begin{prb}
    (C4S2Q8) \\
    The \textit{modulus of continuity} of a function $f : E \to \mathbb{R}$ is the function $\omega(\delta)$ defined for $\delta > 0$ as follows:
    $\omega(\delta) = \sup\limits_{\begin{subarray}{c}|x_1 - x_2| < \delta \\ x_1, x_2 \in E\end{subarray}} |f(x_1) - f(x_2)|$. \\
    Thus, the least upper bound is taken over all pairs of points $x_1, x_2$ of $E$ whose distance apart is less than $\delta$. \\
    Show that: 
    \begin{enumerate}
        \item[a)] the modulus of continuity is a nondecreasing nonnegative function having the limit $\omega(+0) = \lim\limits_{\delta \to +0} \omega(\delta)$;
        \item[b)] for every $\varepsilon > 0$ there exists $\delta > 0$ such that for any points $x_1, x_2 \in E$ the relation $|x_1 - x_2| < \delta$ implies $|f(x_1) - f(x_2)| < \omega(+0) + \varepsilon$;
        \item[c)] if $E$ is a closed interval, an open interval, or a half-open interval, the relation
        $\omega(\delta_1 + \delta_2) \leq \omega(\delta_1) + \omega(\delta_2)$
        holds for the modulus of continuity of a function $f : E \to \mathbb{R}$;
        \item[d)] the moduli of continuity of the functions $x$ and $\sin(x^2)$ on the whole real axis are respectively $\omega(\delta) = \delta$ and the constant $\omega(\delta) = 2$ as $\delta > 0$;
        \item[e)] a function $f$ is uniformly continuous on $E$ if and only if $\omega(+0) = 0$.
    \end{enumerate}
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{sol*}
\begin{enumerate}
    \item[a)] Consider set $W(\delta)=\{|f(x_1)-f(x_2)|\colon |x_1 - x_2| <\delta, x_1, x_2 \in E\}$, then $\forall \delta_1<\delta_2,\ W(\delta_1)\subseteq W(\delta_2)$, then $\omega(\delta_1)=
    \sup\limits_{\begin{subarray}{c}|x_1 - x_2| < \delta_1 \\ x_1, x_2 \in E\end{subarray}} |f(x_1) - f(x_2)|\leq \sup\limits_{\begin{subarray}{c}|x_1 - x_2| < \delta_2 \\ x_1, x_2 \in E\end{subarray}} 
    |f(x_1) - f(x_2)| = \omega(\delta_2)$. \\
    If $f(x)$ is bounded on $E$, then $\exists$ finite $\omega(+0)=\lim_{\delta \to +0} \omega(\delta)$.
    \item[b)] By definition of $\omega(+0)=\lim_{\delta \to +0} \omega(\delta)$, $\forall \varepsilon >0,\ \exists \delta >0$ s.t. $|\sup\limits_{\begin{subarray}{c}|x_1 - x_2| < \delta \\ x_1, x_2 \in 
    E\end{subarray}} |f(x_1) - f(x_2)| - \omega(+0)|<\varepsilon$, expand it. 
    \item[c)] Using: if $|x-y|<d_1+d_2$, then $\exists z$, s.t. $|x-z|<d_1, |y-z|<d_2$. 
    \item[d)] $f(x)=x$: manifestly; $f(x)=\sin(x^2)$: essentially, $\forall \varepsilon >0$, $\exists k>0$, s.t. $\sqrt{(2k+1)\pi}-\sqrt{(2k)\pi}<\varepsilon$. 
    \item[e)] Necessity "$\Rightarrow$": by definition of uniform continuity; Sufficiency "$\Leftarrow$": by contradiction. 
\end{enumerate}
\end{sol*}
\end{minipage}}

\begin{prb}
    (C4S2Q9) \\
    Let $f$ and $g$ be bounded functions defined on the same set $X$. The quantity $\Delta = \sup\limits_{x \in X} |f(x) - g(x)|$ is called the \textit{distance} between $f$ and $g$. It shows how well one function 
    approximates the other on the given set $X$. Let $X$ be a closed interval $[a, b]$. Show that if $f, g \in C[a, b]$, then $\exists x_0 \in [a, b]$, where $\Delta = |f(x_0) - g(x_0)|$, and that such is not 
    the case in general for arbitrary bounded functions.
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{qsol*}
    1. $h(x)=|f(x)-g(x)|$ is continuous on $[a,b]$, then by Weierstrass extreme value theorem ($\sup = \max$). \\
    2. Counterexample: construct using discontinuities.
\end{qsol*}
\end{minipage}}

\begin{prb}
    (C4S2Q10) \\
    Let $P_n(x)$ be a polynomial of degree $n$. We are going to approximate a bounded function $f : [a, b] \to \mathbb{R}$ by polynomials. Let $$\Delta(P_n) = \sup_{x \in [a, b]} |f(x) - P_n(x)| \ \text{and}\ E_n(f) 
    = \inf_{P_n} \Delta(P_n),$$ where the infimum is taken over all polynomials of degree $n$. A polynomial $P_n$ is called a \textit{polynomial of best approximation} of $f$ if $\Delta(P_n) = E_n(f)$. \\
    Show that: 
    \begin{enumerate}
        \item[a)] there exists a polynomial $P_0(x) \equiv a_0$ of best approximation of degree zero;
        \item[b)] among the polynomials $Q_\lambda(x)$ of the form $\lambda P_n(x)$, where $P_n$ is a fixed polynomial, there is a polynomial $Q_{\lambda_0}$ such that $\Delta(Q_{\lambda_0}) = 
        \min\limits_{\lambda \in \mathbb{R}} \Delta(Q_\lambda)$;
        \item[c)] if there exists a polynomial of best approximation of degree $n$, there
        also exists a polynomial of best approximation of degree $n + 1$;
        \item[d)] for any bounded function on a closed interval and any $n = 0, 1, 2, \ldots$ there exists a polynomial of best approximation of degree $n$.
    \end{enumerate}
\end{prb}
\fbox{\begin{minipage}{0.95\textwidth}
\begin{qsol*}
\begin{enumerate}
    \item[a)] $P_0(x)\equiv a_0=\frac{\sup f(x)+\inf f(x)}{2}$.
    \item[b)] $\Delta(Q_\lambda)=\sup_{x\in[a,b]}|\lambda P_n(x)-f(x)|$, then discuss the uniform continuity of $\Delta(Q_\lambda)$ w.r.t. $\lambda$.\\
    \fbox{\begin{minipage}{0.9\textwidth}
        Lemma. $\sup\limits_{x \in [a,b]}|f-g|\leq \sup\limits_{x \in [a,b]}|f-h|+\sup\limits_{x \in [a,b]}|h-g|,\ \forall f,g,h,\ x\in[a,b]$
    \end{minipage}}
    Proof idea: $|f-g|\leq |f-h|+|h-g|$. \\
    Using Lemma, $\forall \lambda_{i},\ i=1,2$, $\sup\limits_{x \in [a,b]} |f(x)-\lambda_1 P_n(x)|\leq\sup\limits_{x \in [a,b]} |f(x)-\lambda_2 P_n(x)|+\sup\limits_{x \in [a,b]}|\lambda_1 P_n(x)-\lambda_2 P_n(x)|$, 
    i.e. $|\Delta(Q_{\lambda_1})-\Delta(Q_{\lambda_2})| \leq \sup\limits_{x \in [a,b]}|\lambda_1 P_n(x)-\lambda_2 P_n(x)| = |\lambda_1-\lambda_2|\sup\limits_{x \in [a.b]}|P_n(x)| = \varepsilon \mathcal{O}(1)$. 
    \item[c)] Hint: $E_n(f)\geq E_{n+1}(f)$, wait to accomplish.
    \item[d)] Induction using c).
\end{enumerate}
\end{qsol*}
\end{minipage}}

%\end{document}